<?php

/*Написать функцию которая на вход получает строку(пароль) и проверяет сложность пароля 
по шкале Простой, Нормальный, Сложный.
Критерии определения взяла предложенные в группе: 
Сложный пароль: больше 8 знаков, содержит латинские буквы в верхнем и нижнем регистре, цифры и спецсимволы.
Нормальный: больше 8 знаков, содержит латинские буквы в верхнем и нижнем регистре, цифры.
Простой: менее 8 знаков, состоит только из символов или только из цифр.
*/

function pass($password) {
    $length = strlen($password);
    if ($length > 8) { 
        if (preg_match("/[a-z]/", $password)) {
            if (preg_match("/[A-Z]/", $password)) { 
                    if (preg_match("/[0-9]/", $password)) {
                         if (preg_match("/[!@#$%^&*()_\-+=]/", $password)) {
                        echo "Сложный пароль";
                        } else {
                        echo "Нормальный пароль";
                        }    
                    }  else {
                    echo "Простой пароль";
                    }  
            } else {
            echo "Простой пароль";
            } 
        }  else {
         echo "Простой пароль";
               }  
    } else {
    echo "Простой пароль";
    }
} 
var_dump(pass("mad9#flfkA"));
